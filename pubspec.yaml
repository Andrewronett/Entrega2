name: alertsapp
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter

  flutter_secure_storage: ^9.2.2
  audioplayers: ^5.2.1
  cupertino_icons: ^1.0.8
  http: ^1.2.2        # 👈 Para hacer peticiones al backend (login, registro, etc.)
  provider: ^6.1.2    # 👈 Para manejar estado y mantener sesión de usuario

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/audio/ # Asegúrate de crear esta carpeta y agregar los archivos mp3 necesarios

